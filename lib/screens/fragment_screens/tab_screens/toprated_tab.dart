import 'package:flutter/material.dart';

class TopratedTab extends StatelessWidget {
  const TopratedTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is Top rated Page'),
    );
  }
}
