import 'package:flutter/material.dart';

class HotDealsTab extends StatelessWidget {
  const HotDealsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is hot deal page'),
    );
  }
}
