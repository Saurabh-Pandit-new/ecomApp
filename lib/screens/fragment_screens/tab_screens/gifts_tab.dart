import 'package:flutter/material.dart';

class GiftsTab extends StatelessWidget {
  const GiftsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is giftPage'),
    );
  }
}
